{
  "name": "aguaspl-frontend",
  "version": "1.0.0",
  "description": "Sistema de Gestión de Agua - Frontend",
  "main": "index.html",
  "scripts": {
    "prestart": "node scripts/generate-env-config.js",
    "start": "serve -s . -l $PORT || serve -s . -l 3000",
    "build": "echo 'No build step required for static site'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

